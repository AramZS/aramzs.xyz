{% extends "./page.njk" %}
{% set ogtype = 'article' %}
{% set subTitle %}
    posted on <time datetime="{{ page.date | isoDate }}">{{ page.date | dateToFormat("DDD") }}</time> in: {% include "../components/tag-list.njk" %}.
{% endset %}

{% block pageContent %}
    <section data-source="src/_includes/layouts/page-resource.njk">
        {{ content | safe }}
        <small>&mdash; {{ cite.name }} | <a href="{{ cite.href }}">link</a></small>
    </section>

    {% include "components/backlinks.njk" %}
{% endblock %}
